# Copier ce fichier en `.env` puis remplacer les valeurs.
# Le `.env` n'est jamais versionne (voir .gitignore).
# Docker Compose lit automatiquement le `.env` place a la racine du projet.

POSTGRES_DB=bibliotheque_db
POSTGRES_USER=changeme_user
POSTGRES_PASSWORD=changeme_mot_de_passe_fort
